(function(){
  "use strict";

  // PC / mobile rendering is selected by the server from the real request
  // (User-Agent), or by an explicit /mobile URL / ?view=... preference.
  // Browser viewport changes are presentation-only and must never navigate.
})();
